name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
      - name: Cache zooid build
        uses: actions/cache@v4
        with:
          path: .cache
          key: zooid-${{ hashFiles('ZOOID_VERSION') }}
      - name: Build zooid (pinned)
        run: make zooid
      - name: Unit tests
        run: make test
      - name: Integration tests
        run: make test-it
      - name: Case coverage
        run: make test-coverage-cases
      - name: Build
        run: make build
